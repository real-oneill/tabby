pygame-ce>=2.5
numpy>=1.26
sounddevice>=0.4
PyGuitarPro>=0.9   # parse .gp3/.gp4/.gp5 for the tempo-synced tab player

# AI assistant (Pi-side, optional — the app degrades gracefully without these)
pywhispercpp>=1.2  # on-device speech-to-text (whisper.cpp); needs a model, e.g. base.en
shazamio>=0.4      # Shazam-style song identification
audioop-lts; python_version >= "3.13"  # shazamio/pydub needs audioop (removed in 3.13)
