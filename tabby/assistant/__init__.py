"""AI assistant: voice-driven agent client, song-ID, and the ASSIST screen.

The agent itself runs on Databricks (see the repo's top-level `agent/` directory).
This package is the Pi-side glue: capture input, call the agent, run its actions.
"""
